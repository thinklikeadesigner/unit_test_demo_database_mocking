import 'package:flutter/material.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'database mock testing Demo',
      theme: ThemeData(
          // Define the default brightness and colors.
          ),
    );
  }
}

// 30D4D4
